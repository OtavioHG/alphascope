[build-system]
requires = ["maturin>=1.6,<2.0"]
build-backend = "maturin"

[project]
name = "alphascope-rust"
version = "0.1.0"
description = "Rust acceleration layer for AlphaScope"
requires-python = ">=3.10"
